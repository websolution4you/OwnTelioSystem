services:
  - type: web
    name: own-telio-system
    runtime: docker
    plan: starter
    healthCheckPath: /health/live
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: DRY_RUN
        value: "true"
